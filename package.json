{
  "name": "stonecutter_frontend",
  "version": "1.0.0",
  "description": "Build all the things",
  "main": "server.js",
  "engines": {
    "node": "0.12.7"
  },
  "scripts": {
    "preinstall": "npm config set bin-links false",
    "postinstall": "./npm-postinstall.sh"
  }
}
